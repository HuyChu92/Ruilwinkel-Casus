name: .NET

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2.1.0
      - run: dotnet test

        


